linters-settings:
  revive:
  # Configura revive si es necesario. Revive es altamente configurable.
  govet:
    enable:
      - fieldalignment

linters:
  disable-all: true
  enable:
    #- revive
    - unused
    #- govet
    - exportloopref
    - exhaustruct

issues:
  exclude-use-default: false
  exclude-files:
    - "_test\\.go$"
